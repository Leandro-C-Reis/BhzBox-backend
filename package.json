{
  "name": "boxvendas-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev src/server.ts --ignore__tests__ --transpile-only --ignore-watch node_moduless",
    "pretest": "set NODE_ENV=test && knex migrate:rollback --all && knex --knexfile knexfile.ts migrate:latest ",
    "test": "set NODE_ENV=test && knex --knexfile knexfile.ts seed:run && jest --runInBand --detectOpenHandles",
    "posttest": "set NODE_ENV=test && knex migrate:rollback --all",
    "doc": "node docs/aglio.js",
    "migrate": "knex --knexfile knexfile.ts migrate:latest",
    "rollback": "knex migrate:rollback --all",
    "postrollback": "knex --knexfile knexfile.ts migrate:latest",
    "seed": "knex --knexfile knexfile.ts seed:run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "knex": "^0.21.12",
    "mysql2": "^2.2.5",
    "pg": "^8.5.1",
    "xml-js": "^1.6.11"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/cors": "^2.8.8",
    "@types/express": "^4.17.9",
    "@types/jest": "^26.0.18",
    "@types/supertest": "^2.0.10",
    "jest": "^26.6.3",
    "supertest": "^6.0.1",
    "ts-jest": "^26.4.4",
    "ts-node": "^9.1.1",
    "ts-node-dev": "^1.0.0",
    "typescript": "^4.1.2"
  }
}
